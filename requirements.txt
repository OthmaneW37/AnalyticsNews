# ============================================================
# News Pipeline — Toutes les dépendances (Phases 1 à 4)
# ============================================================

# Phase 1 — Scraping & Data Lake local
requests==2.31.0
feedparser==6.0.11
beautifulsoup4==4.12.3
lxml>=5.3.0
pandas>=2.2.3
pyarrow>=18.0.0

# Phase 2 — Infrastructure distribuée (Docker)
kafka-python==2.0.2
minio==7.2.7

# Phase 2bis — Qualité & Enrichissement
langdetect==1.0.9
duckdb==0.10.2

# Phase 3 — Différenciateurs (BERTopic + Polymarket)
bertopic>=0.16.4
sentence-transformers==2.7.0
torch --index-url https://download.pytorch.org/whl/cpu

# Phase 4 — API (FastAPI)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
